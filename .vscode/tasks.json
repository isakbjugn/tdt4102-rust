{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build",
      "type": "shell",
      "command": "clang++ -std=c++20 -g -Wall -Wextra ${file} -o ${fileDirname}/${fileBasenameNoExtension}",
      "group": "build"
    
    },
    {
      "label": "build & run",
      "type": "shell",
      "command": "clang++ -std=c++20 -Wall -Wextra ${file} -o ${fileDirname}/${fileBasenameNoExtension} && ${fileDirname}/${fileBasenameNoExtension}",
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
